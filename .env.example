# Port configuration for Docker Compose
# Copy this file to .env to customize port settings

HOST_PORT=8088        # External port (what you connect to from outside)
CONTAINER_PORT=8088   # Internal port (overrides config.yaml via SERVER_PORT env var)

# Examples:

# Run on a different port:
# HOST_PORT=9000
# CONTAINER_PORT=9000

# Traditional setup (if you need port 8080):
# HOST_PORT=8080
# CONTAINER_PORT=8080
